apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws-ipfs-bitswap-peer.fullname" . }}
  labels:
    {{- include "aws-ipfs-bitswap-peer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aws-ipfs-bitswap-peer.selectorLabels" . | nindent 4 }}
